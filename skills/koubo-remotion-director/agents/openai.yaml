interface:
  display_name: "口播 Remotion 导演"
  short_description: "串联 V8 包装与摄影级纸艺候选，保留实录和正式门禁"
  default_prompt: "使用 $koubo-remotion-director 先以真实口播和权威时间轴建立 V8 visual-plan；需要解释抽象机制时，在独立 revision 中生成 plan-only 的摄影级纸艺导演计划，先验收静帧和 WithSfx/NoSfx 候选，再申请正式生产。"
