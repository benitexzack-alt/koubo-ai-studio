# 口播项目 AGENTS.md

> 适用范围：本目录下所有口播选题、脚本、素材处理、转写、剪辑、包装、发布复盘。
> 项目根目录：以本 `AGENTS.md` 所在的 Git 仓库根目录为准，不依赖固定用户名或克隆位置。
> 建立日期：2026-07-04

## 一、强制读取顺序

任何 AI 处理本项目任务前，必须先读取：

1. `project.md`
2. `knowledge/00-项目知识索引.md`
3. `knowledge/01-账号战略与事业主线.md`
4. `knowledge/02-个人事实与公开边界.md`
5. `knowledge/03-口播执行守则.md`
6. `knowledge/04-内容生产SOP.md`
7. `knowledge/05-合规隐私与证据规则.md`
8. `knowledge/06-选题脚本与风格规范.md`
9. `knowledge/14-超哥口播声音档案.md`
10. `knowledge/15-抖音精选内容质量验收.md`
11. `knowledge/20-V8连续语义动效与可感知音效基线.md`、`workflow/active-production-profile.v1.json` 与 `knowledge/21-超哥口播语言与重复硬门禁.json`。V8是新视频默认制作基准；V7.2只用于锁定母版回归和故障回滚，任何降级必须有用户明确批准记录。公开正文还必须通过项目语言失败样本和 `workflow/recent-content-history.v1.json` 历史锚点扫描，不得用 `copy_review` 自报布尔值替代。
12. `skills/source-essence-synthesis/SKILL.md`。
13. `skills/content-brain-gate/SKILL.md` 及 `skills/content-brain-gate/references/public-content-gate.md`。
14. 如果 `KOUBO_PERSONAL_KB` 或项目同级的 `../个人知识库` 存在，必须额外读取该知识库 `AGENTS.md` 和现行《公开内容生产大脑硬门禁》；当其与项目便携快照冲突时，以个人知识库现行版为准。
15. 当前账号实测学习卡：真实账号任务读取 `<personal-kb>/01_项目实战/抖音知识中台/工作区/2026-08-09-超哥AI创业记账号数据复盘/当前账号实测学习卡.json`，记录当前 SHA-256；不能用聊天记忆、复盘摘要或 Skill fixture 替代。
16. 最近六条已发布、已拍摄或已确认内容的真实转写或确认稿。
17. 与当前任务直接相关的原始资料，并核对每条来源的完整度和允许用途。

完整访谈、长视频、文章或参考口播的深度改编，必须先用项目内 `source-essence-synthesis` 生成并校验源头精髓卡；达到 `ready-for-outline` 后，再使用 `content-brain-gate` 生成并校验内容门禁卡。普通原创单点口播可直接进入内容门禁。两项能力都可通过 `tools/setup-koubo.mjs` 注册到 Codex。没有实际读取证据或校验结果时，不得只说“已经查过知识库”。

## 二、项目真实目标

用真实口播和真实企业服务过程，建立“甘肃兰州 AI 创业者”的个人账号。

账号不是讲抽象概念，而是服务甘肃本地小微企业、个体老板、创业者和 OPC 一人公司，让他们看懂、用上、验证 AI 工具。

阶段首要成果：

```text
先跑通 1 条真实口播样片
→ 固化口播模板
→ 每周稳定生产约 5 条
→ 用内容获得有效咨询和本地企业线索
```

## 三、最高原则

- 事实高于流畅表达。
- 未验证，不说死。
- 不虚构经历、客户、收入、证书、行业数据和效果。
- 一条视频只解决一个核心问题。
- 先完成真实最小闭环，再做复杂包装和自动化。
- 重大选题、行业结论、政策和数据必须查真实信源，优先中国信源。
- 密钥、证件、合同、财务、客户隐私不得进入公开素材。

## 四、固定工作流

每条视频必须按顺序执行：

1. 明确选题目标、服务对象、看完要改变的真实状态和当前证据边界。
2. 查知识库、当前账号实测学习卡、最近六条、本人声音档案和必要外部信源。
3. 建立 `performance_feedback`：绑定学习卡当前 SHA-256，选择本条采用的有效学习，声明开头时间计划、时长理由、主辅指标、完整观察窗口和可证伪假设。
4. 标记每条来源的完整度和允许用途；`metadata-only` 只能作为线索。
5. 如果任务要求从完整参考内容保留主要观点、知识点或叙事精髓，先运行 `source-essence-synthesis`：建立母命题、至少 6 个认知节点、连通论证链、纠偏账本和普通人三层所得；未达到 `ready-for-outline` 时停止套结构或写稿。
6. 完成最近六条查重、第一性原理选题卡和核心对象机制卡。
7. 建立 `douyin_quality` 质量卡，至少把获得感和表达力设为主目标；明确惊喜感、感染力的真实证据和不夸张边界。
8. 运行 `content-brain-gate`；未达到 `ready-for-draft` 时停止写稿并补资料或换选题。
9. 写脚本，建立事实锁，标出四项质量特征在正文中的证据，自动调阅 `humanizer-zh` 与 `humanize-koubo-script`，执行模式扫描、事实安全精修、留存风险审稿、大声朗读、本人声音、账号实测反馈和重复检查。
10. 每份提交用户确认的完整口播稿，末尾必须同时附带抖音主标题、两个备选标题和一份全中文 `3:4` 封面提示词；标题与提示词必须沿用当前事实锁和双 Skill 审稿结果。用户确认脚本、质量目标和策略后，状态才可升级为 `ready-for-production`。
11. 素材盘点和技术检查。
12. 转写，缓存逐字稿。
13. 内容诊断：口误、重复、停顿、关键信息、可删项。
14. 判断是否需要内容剪辑：只有用户明确要求删除、压缩、重排、处理重录或口误时，才进入剪辑策略、粗剪和粗剪确认。
15. 完整单条口播默认全量保留，直接进入视觉包装方案；词级转写只用于字幕、语义卡和运镜定位。
16. 字幕和信息卡制作；脚本阶段已经交付标题与封面提示词，成片后只按最终画面补充必要修订。封面不再由项目自动生成，用户在外部工具中自行生成和选择。
17. V4/V5/V6 包装、数字运镜、音效和低清预览。
18. 自动质量检查。
19. 正式成片。
20. 从本条正式成片的本人真实口播画面中截取一张推荐封面人物图，并一次性整理 `3:4` 真人截图合成封面提示词、一个主标题、两个备选标题、抖音发布文案和话题；全部写入发布记录并通过 V8 完整交付包校验。
21. 用户人工观看确认。
22. 发布后按早期、24 小时、72 小时和 7 天窗口归档数据；更新复盘报告与当前学习卡，达到学习卡规定的最小受控样本前，不把单条表现升级为稳定规律。

未经 `ready-for-production`，不得开始分镜、V4/V5/V6、动效、音效、封面和正式成片。后期能力不能替代内容门禁。

脚本固定交付包中的“文字版封面提示词”不等于已经启动封面制作，但只能在事实锁、双 Skill 和 `ready-for-draft` 均通过后生成。缺少抖音标题或封面提示词的文稿只能标记为 `incomplete-delivery`，不得称为完整口播稿。

V8正式成片的最低完整交付固定为：`成片 + 本条成片真人截图 + 3:4封面提示词 + 一个主标题和两个备选标题 + 抖音发布文案 + 抖音话题`。截图必须记录本条正式成片路径和时间点，不得用历史照片、其他视频帧或AI重绘人物替代。缺少任一项时，成片交付同样只能标记为 `incomplete-delivery`。

抖音精选内容四项特征只作为质量目标，不能替代事实、来源、合规、账号阶段和用户确认，也不能被表述为入选保证或平台算法公式。

账号实测反馈是选题和文稿的硬输入，不是可选参考。`performance_feedback` 缺失、学习卡未读或哈希过期、采用的学习 ID 无效、开头时间计划越界、时长无证据理由、指标或观察窗口不完整时，状态必须为 `blocked`。不得用一次爆或一次不爆推出平台规律，也不得把“长内容表现差”机械等同于“所有稿件必须短”。

未经用户明确要求，不得把粗剪设置为完整单条口播的固定交付或确认门禁。涉及内容删改时仍须确认剪辑策略和粗剪；全量保留时直接生成 V4 成片并在正式发布前由用户完整观看确认。

### 文案处理双 Skill 硬门

凡是生成、续写、改写、压缩、扩写、润色或重组用户将要公开口播的标题、钩子、正文、转场、金句、结尾和行动引导，都必须在本轮任务中自动调阅以下两项能力，不得等待用户再次提醒：

1. `${CODEX_HOME:-$HOME/.codex}/skills/humanizer-zh/SKILL.md`：只作 AI 写作模式检查，不负责生成事实。
2. `<project-root>/skills/humanize-koubo-script/SKILL.md`：负责事实安全精修、本人声音复核和留存风险审稿。

固定顺序：

```text
内容门禁达到 ready-for-draft
→ 建立事实锁
→ 读取两项 Skill 的当前版本
→ humanizer-zh 模式扫描
→ humanize-koubo-script 事实安全精修
→ 强制执行留存风险审稿
→ 生成绑定当前稿件哈希的 copy_review 报告
→ 机器校验
→ 才能把文稿交给用户审阅或申请 ready-for-production
```

硬性约束：

- “留存风险审稿”对口播文案处理不再是可选分支；最多指出三个真实高风险节点，没有高风险节点时必须写明理由，禁止凑数。
- 仅做逐字转写、字幕时间轴、官方原文摘录或用户已确认原话的原样搬运，不属于文案改写；一旦把这些内容整理成公开口播稿，立即恢复双 Skill 硬门。
- 只写“已去 AI 味”“已完成初检”或手填 `humanize_passed: true` 不算执行证据。
- 每份提交用户审阅的文稿都必须有 `copy_review.report_path`；报告必须记录两项 Skill 的实际路径与当前 SHA-256、当前文稿 SHA-256、事实差异、留存风险结果和五项评分。
- 报告缺失、Skill 哈希过期、报告绑定的不是当前稿件、事实保真不是 `10/10`，状态必须为 `blocked`。

## 五、当前项目规格

- 第一条测试视频按用户要求先做 `16:9`。
- 第一版输出建议：`1920x1080`、`30fps`、H.264、AAC。
- 后续如面向抖音/视频号/小红书主发布，默认再做 `1080x1920` 竖屏版本。
- 原始素材只读，不覆盖、不删除、不重命名。

## 六、素材与目录规则

- `source/`：原始素材副本，只读。
- `refs/`：参考账号截图、风格参考。
- `edit/transcripts/`：转写结果。
- `edit/verify/`：抽帧、波形、质检材料。
- `edit/exports/`：低清预览和中间导出。
- `outputs/`：最终交付成片。
- `knowledge/`：项目知识库和执行守则。

只处理用户明确指定的素材目录，不扫描无关桌面、下载、证件、合同、财务资料。

## 七、API 与隐私

- ElevenLabs 转写会上传提取出的音频，必须有用户明确授权。
- API Key 只能进入本机 `.env` 或环境变量，不得写入文档、脚本、字幕、Git。
- 不在终端、日志和聊天中回显完整密钥。
- 生图 API 第一阶段非必需；除非封面或视觉资产需要，并经用户确认。

## 八、内容边界

- 个人经历以 `knowledge/02-个人事实与公开边界.md` 为准。
- “人工智能训练师高级培训证书”可准确公开。
- “生成式人工智能 AIGC 技术应用证”在原件未补齐前，只能说“本人确认，待原件核验”，不得公开包装成已核验证书。
- “西北首家”“行业第一”“保证获客”“成熟体系”等绝对化表达，必须有证据，否则禁止使用。
- 金融、小额贷款、RWA 等经历只做中性、合法、风险反思表述，不做金融承诺和收益暗示。

## 九、完成用语

- 只完成目录或配置：说“已整理/已配置”。
- 只完成技术检查：说“技术检查完成”。
- 只完成预览：说“预览已生成，待人工确认”。
- 只有真实素材完成转写、剪辑、包装、自动质检和用户确认后，才能说“这条口播成片通过验证”。

## 十、复盘

重要阶段结束后，更新：

- `project.md`
- `knowledge/08-决策与复盘日志.md`
- 如果已定位个人知识库，必要时更新其 `06_每日复盘/YYYY-MM-DD.md`；如果不存在个人知识库，只更新项目 `knowledge/08-决策与复盘日志.md`。
