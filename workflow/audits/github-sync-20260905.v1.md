# 2026-09-05 GitHub 同步核查

## 初始状态与同步目标

- 现有仓库：`benitexzack-alt/koubo-ai-studio`，默认首页分支为 `main`。
- 核查时远端 `main` 为 `594d182`（2026-07-18），开发分支为 `680adac`，本地已提交版本为 `db95577`；`main` 到本地版本可快进，落后 280 个提交，没有分叉。
- 本轮同步已完成的内容流程、V8 生产基线、V9 导演试点、项目 Skill 和必要编译依赖；重写 README、更新简介并补更新记录。使用普通快进推送，不改写历史。
- V9.1 自动匹配与验收经验闭环由另一任务同时施工；未验收的进行中修改不属于本轮稳定快照，不暂存、不覆盖。

## 精确收录与保留边界

- 增补 RunningHub 批量 Skill 的原样维护快照；安装器默认注册 8 项，可选注册第 9 项，不覆盖同名独立安装。
- 补齐已跟踪组件实际导入的音效数据和样式合同，以及当天组件需要的中文单语字幕选项；默认双语行为不变。
- 修正媒体组件的 `trimBefore` 参数兼容性，发布工具改用项目根目录、用户目录或显式环境变量。
- 测试改用合成资料与隔离上下文验证路径、哈希和阻断合同，不把合成记录写成真实账号验收。
- 初始工作区存在大量原片、预览和未跟踪中间产物，约 19 GB；不整体收录。旧文稿、未完成的历史门禁回执和无关视觉调整保留本机，不使用 `git add -A`。
- 未增加统一开源许可证，未收录 `.env` 或密钥，未启动外部素材上传、付费生成或平台发布。

## 验证方式与已确认结果

- 从 Git 暂存树导出独立源码目录，不复制未跟踪源文件、个人知识库或原始媒体；仅复用本机已安装的 Remotion 依赖。这不等于在空白机器重新执行 `npm ci`。
- 独立源码的 TypeScript 检查和空媒体目录 Remotion 打包通过。
- 最终干净源码便携回归：143 项通过、0 项跳过、0 项失败，涵盖语法、9 项 Skill 结构、源头和内容门禁、账号预检、隔离安装、冲突保护、无上传预检及 Remotion 打包；其中内容门禁单元回归 54 项通过，包含历史文件缺失与哈希漂移阻断。
- 临时 Codex 目录内注册 Skill 后，V9 活动导演档案、包哈希和链接验证通过；离线体检为 41 项通过、6 项环境提示、0 项失败。没有替换本机真实 Skill 链接。
- Shotcraft 回归 90 项通过；平台安全区与 V9 合同专项 8 项通过；首帧、实录、生产与 release 合同专项 7 项顶层测试通过。
- 导演与生成视频回归的 TAP 汇总为 46 项通过；其中两个依赖外部不可变验收根的测试内部声明跳过（`test-request-isolation.mjs`、`test-supervisor-a-acceptance-gate.mjs`），不计为真实验收。
- 暂存差异格式、常见密钥模式与大文件检查用于提交前核查；它们不是完整安全审计，也不能替代素材权利和人工审核。

## 不得扩大的结论

V8 仍为活动正式生产档案。V9 当前仅为下一条新视频试点，尚未完成真实素材端到端和用户完整观看终验。本轮没有重渲染业务视频，也没有改变历史发布状态。

Shotcraft 预览脚本变更会使绑定旧脚本哈希的授权回执失效；新预览必须重新获得当前任务授权，不回填或篡改旧回执来通过门禁。

推送是否完成以 GitHub 分支实际提交为准，最终提交哈希与远端复核结果记录在唯一知识库的当天复盘。
