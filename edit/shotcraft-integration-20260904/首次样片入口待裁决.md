# 首次样片入口待裁决

2026-09-04。当前事实以 `lock-repair-and-preview-entry-audit.v2.json` 为准；v1历史回执保持不变。

## 已完成

- 核对37e0399、8c7ba21两次OCR修复，确认此前是已提交源码与旧锁不一致。
- 修正逐标签即时OCR回执未验证最终合成图的问题。所有标签装配后再逐项识别最终图，任一失败均不输出成功图和回执。
- 合成双标签正反例、本地OCR到投递清单、预拍合同与锁回归通过；未重生成或覆盖业务图片。
- 导演锁同步为71文件，包SHA-256为 `babbfe9fd0c9df57affcf079cfaba952b79cb5c33c239e0a3c5711f9bd0daec8`；活动导演档案校验通过，默认正式资格仍为false。
- Shotcraft的38项测试、隔离类型检查和31秒计划验证通过。这些不等于已生成或看过样片。

## 新发现的入口矛盾

用户已经授权制作本条31秒样片。缺的不是重复询问制作许可。

现有生产级预览入口先要求 `automation-handoff-eligible` 导演合同；该合同又要求候选视频、技术QA、正常速度完整观看和A/B文件哈希。对于同一条首次候选，形成“先生成并验收才允许生成”的循环。

另外，系统独立Ed25519信任根未配置、验收注册表为空。仅写授权JSON或让执行者自签都不能满足真实独立授权。补齐签名也不会消除上述首次候选循环。

已有监督任务处于归档状态，工具拒绝投递核查请求，本轮没有擅自恢复该任务。两位代码审计者提供的只读审查不冒充系统签名或用户观看。

## 建议的最小下一步

单独批准首次隔离效果样片的受控入口设计。边界限定为本条31秒/930帧、有声与无附加音效两个输出、当前原片及代码哈希；不复用正式生产许可，不改共享V8组件，不改变纸艺默认路线。

正确顺序为：渲染前用户许可和独立代码审查 → 受控样片 → 解码/声画/避让QA → 用户正常速度完整观看。正式生产保持关闭。

该入口的授权信任方式需要一并明确：沿用现行V2时必须由独立管理者安装公钥信任根并独立签名；若决定采用本地实验专用授权，则需显式批准其与生产V2的边界，不能暗中旁路。

当前未实施入口改造，未渲染样片、未新增密钥、未修改注册表、未上传、未付费、未发布。主任务等待上述架构范围裁决，不需要用户重新提供素材。
