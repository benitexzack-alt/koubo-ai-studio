# 可选动效库接入

状态：候选实现已建立；实录样片停在渲染前。不是已生成视频，不是正式交付，不替换现有纸艺或 V8。

## 本次产出

- 本地固定上游目录：157张动效卡、214种风格，保留Apache-2.0许可证与来源哈希。不引入上游音频、字体、模拟后台或预览视频。
- 五项候选适配：关键词划线、逐项呈现、真实画面框选、线条连接、纸胶带固定。其他卡片仅可检索参考。
- 入口：`skills/koubo-shotcraft-library/SKILL.md`，已加入项目知识索引。默认关闭，需按语义选择，不是每期必须使用。
- 隔离样片：`remotion/src/shotcraft-candidate-v1/index.tsx`，使用本期实录42.600–73.600秒，计划31秒/930帧。同画面有/无附加音效两个composition，尚未执行渲染。
- 真实案例仅证明所提供作品中的可见内容，不能证明收入、客户关系或市场需求。框选不用于纸艺或AI情景演绎分支。

## 验证范围

本库合同测试、组件确定性测试、独立类型检查、60个合成浏览器布局状态已运行，明细以 `integration-receipt.v1.json` 和日志为准。

`component-qa` 图片仅为无真人的合成组件测试，不能冒充用户实录样片。未完成真实声画、逐词卡点、素材混音、人脸避让和全片解码验收。词级卡点采用原ASR参考，不把按字均分时间冒充声学对齐。

未改写共享 `V72ProductionShell.tsx`、`V8SemanticStage.tsx`、公共Root、活动V8档案、纸艺主路由或原导演Skill。原目录现有改动保持原样。

## 渲染阻断

现有导演锁校验发现 `bake-firstframe-text.mjs`、`test-firstframe-text-bake.mjs` 与锁不一致，并有未登记的 `recognize-text-vision.swift`。这些是本次动效接入前已存在的状态，不应在新增效果时顺手接纳。

V2受控入口的独立授权注册表为空，系统独立信任根不存在。本任务不会生成自己的监督签名或借历史候选绕门。原维护/监督方补齐后，再创建本条受控job及当前知识上下文、素材与代码绑定，进行实录样片。

## 后续验收

1. 先恢复旧导演锁与独立授权链的真实可核验状态。
2. 实际生成31秒A/B样片，检查框选目标、字幕与嘴型、原声完整性、音效尾部、人物小窗进出和安全区。
3. 用户正常速度观看后逐效果确认；只确认过的效果可申请用于新片。本条已完成成片不重渲染、不自动发布。
