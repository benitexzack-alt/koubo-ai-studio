# 兰州本地 AI 服务纸艺素材交接 R2

## 本次修订

- 删除“家庭故事从素材到专属动画”纸艺镜头。
- 删除“婚礼定制短片的制作链”纸艺镜头。
- 两处改用用户提供的真实原片，不再消耗首帧与图生视频额度。
- 纸艺镜头由 9 段缩减为 7 段，并重新连续编号为 P01-P07。

## 两个独立输入

1. `first-frame-prompts.v1.json`
   - 只给首帧生图任务读取。
   - 只含静态场景提示词，不含图生视频动作。
   - 当前首帧批次已建立，尚未生图；样图门为 P01、P03、P07。
2. `runninghub-image-to-video-prompts.v1.json`
   - 只给 RunningHub 图生视频批量任务读取。
   - 只能在对应首帧已经生成、写入中文并通过验收后使用。
   - 当前不能直接生成视频，因为首帧图片尚不存在。

人工复制图生视频提示词使用 `runninghub-image-to-video-prompts.md`。

## 约束

- 两个 JSON 不得混用。
- RunningHub 站内 Agent 对话不是正式批量入口，不要把本地路径直接发给站内 Agent。
- 拍摄完成后仍需依据实际口播执行 post-shoot 重绑。
