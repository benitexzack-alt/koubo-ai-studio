# B 方案布局阶段发现跨坐标合同冲突

本轮没有调用生图模型。两版参考为本地生成的构图草图，不能作为首帧交付。

当前结论：需要先修导演合同，再恢复图片生产。

物理合同规定同一纸叠含净空的 A1 横向包络是 53—227 毫米，A4 左端是 293 毫米；二维动作区却要求 A1 落在 192—806.4 像素、A4 从 1075.2 像素以后开始。用同一水平线性比例换算时：

- A1 能放下，比例必须不大于 3.5311 像素/毫米；
- A1 与 A4 的间隔能对上，比例又必须不小于 4.0727 像素/毫米。

两者矛盾。这证明当前统一线性布局方案不可行，不等于已证明所有透视构图都不可能。

导演已独立核对并承认：此前分别验证了毫米物理合同和二维动作区，却没有验证二者是否来自同一映射。143 项旧回归没有覆盖这一问题。此前把问题概括成“模型不服从布局”不够准确，本次新增证据将根因收敛到上游缺少跨坐标联验。

v1/v2 布局参考虽然改善安全边距和人物避让，但仍不符合原动作区，均不可送模型。旧图片与旧签发文件未改；本轮首帧、写字、上传、视频提交均为零。

建议的唯一最小后续：由导演在新 revision 建立统一投影，从完整物件包络推导二维动作区，补一条能拦住本次真实冲突的回归，再做独立复核与编译。文稿、四个中文节点、物件数量、动作顺序和时长不变。导演确认这属于用户既有修复及本轮B方案授权内的必要技术修正，已接手新导演r3；不再重复询问用户。本执行attempt保留，本任务不暗改旧签发或代改导演源码。
