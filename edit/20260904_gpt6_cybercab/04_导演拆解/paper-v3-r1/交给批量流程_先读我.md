# 本期六段纸艺首帧交接

接收任务：简化RunningHub批量流程（01a05ce8-84ee-7112-bbc5-309c3e660c77）。

用户授权：把本期六段纸艺动画交给批量流程任务制作。本次先执行带字首帧样图，不自动提交付费视频生成。

唯一工程仍通过桌面 `口播素材/2026-09-04_GPT6与Cybercab` 打开。本批输出只在本文件所在目录保存，不复制第二套工程、不复用上一期批次。入口中的 `05_首帧图片` 暂不再存同一批副本。

## 已验证输入

| 文件 | SHA-256 |
| --- | --- |
| first-frame-prompts.v1.json | 59fa6ed48063e43d100c7a1771cef9e5faa83c1201ca4253861439bc984acf78 |
| runninghub-image-to-video-prompts.v1.json | 4b0131cc1622fefeae408fd809a43e7e955639a5251c59c15a5cf16ca33e5180 |
| director-validation-receipt.v1.json | e834e193f81ffe14d269665e45c4bbc99c8ae7705697a43d48493030baf6fedd |

本条新建 request、route lock、plan、compile receipt 和 validation receipt 齐全。验证回执 `skillExecuted=true`；纸艺计划仍为 `validated-provisional-previsualization`，不是生产或成片验收。

## 六段清单

| 镜头 | 主题 | 建议动态图时长 | 四张纸面标签 |
| --- | --- | --- | --- |
| P01 | 从问答到软件操作 | 7秒 | 一问一答、电脑、浏览器、业务软件 |
| P02 | 任务到交付 | 8秒 | 业务任务、操作软件、排查问题、交付结果 |
| P03 | 传统驾驶中的人 | 7秒 | 人开车、人判断、人负责、人决策 |
| P04 | 软件与现实两条路径 | 7秒 | 软件系统、商业工作、现实世界、执行身体 |
| P05 | 人的工作不能消失 | 7秒 | 决策、判断、服务、经营 |
| P06 | 四类服务工作 | 6秒 | 营销策划、视觉设计、SOP搭建、数据复盘 |

总计42秒，均为16:9。这是素材长度，不是剪辑时窗。不得冻结末帧、拉伸或把六段首尾连成完整口播。

## 执行顺序

1. 读取当前 `koubo-paper-firstframe-producer`，核对以上文件和哈希，创建本批唯一 `first-frame-batch.v1.json`。
2. 首轮样图选 P01、P02、P05，分别压力测试左右对照、连续流程、中心人物与资源关系。六镜在合同中均归入复杂解释型，样图不能宣称已覆盖所有纸艺原型。
3. 各镜独立调用内置生图工具，最多两路并发。不得把多镜拼进一次提示词，不自动重试，不改核心物件和数量。保存实际执行提示词与来源文件哈希，防止串镜登记。
4. 无字基础图通过物件数量、分组、空间层次和语义检查后，逐图观察实际纸面，校准四角。计划坐标只是候选，不能直接拿来写字。
5. 用当前确定性写字流程把四个目标词写入各自物件的纸牌，执行最终合成图反透视中文OCR和人工可读性检查。文字不能漂在纸面外、错组、串牌或被裁切。
6. 交付三张带字原图、带字联系表、逐图哈希与质检回执，状态为 `candidate-stills-awaiting-user-review`。用户确认三张带字样图后才生成其余三张。
7. 六张全部通过并经用户确认后，才可生成手工图生视频交接包。本次不登录、上传、提交、排队或付费RunningHub，也不生成视频或公开发布。

批次准备命令（由接收方执行一次）：

```bash
node skills/koubo-paper-firstframe-producer/scripts/prepare-firstframe-batch.mjs \
  --manifest edit/20260904_gpt6_cybercab/04_导演拆解/paper-v3-r1/first-frame-prompts.v1.json \
  --director-receipt edit/20260904_gpt6_cybercab/04_导演拆解/paper-v3-r1/director-validation-receipt.v1.json \
  --project-root /Users/pc/Documents/口播 \
  --sample P01,P02,P05
```

## 不可混淆的边界

- 最终交付是带字首帧；无字基础图只是中间产物。首帧中文由本地确定性工具写入，不要求生成模型猜中文字；之后图生视频只保持字形、纸面和物件身份并完成指定动作。
- P02资料归集、P05资源连接等无标签组已声明其视觉表达及原因，不添加第五张牌；原话仍在口播与字幕，不能把四张牌当成删减口播的许可。
- P03是传统驾驶职责，“人负责”不是用户要求删除的“没有人负责”。已剪除的原话不得重新加入。
- P04上下两条路径始终隔开，不表示GPT直接控制Tesla，不把类比当成AGI成熟证据。P06只展示工作类别，不生成获客、收入或效果保证。
- 本期已经拍摄。既有编译器的 `pre-shoot` 仅作为素材预制合同；request已明示拍摄状态和原片/剪后原片/EDL/原始ASR绑定。字幕、时点和可见文字仍须实录复听及 `post-shoot` 重绑，不声称已经逐字对齐。
- 四张标签在首帧已存在；阶段字段描述物件动作落点，不能冒充文字首次出现的逐帧证据。
- 原片事实审阅、第三方素材采用、全片字幕及新动效审片仍未闭合；不以本批素材授权解冻正式渲染。

## 本轮验证

- 六段 request 编译与独立 validation：通过。
- 导演预制合同、首帧批次、确定性写字到手工交接三组自动测试：3/3通过。
- 以上是合同与测试数据的验证，不是本批图片、中文OCR或视频验收；本批实际生图尚待接收方执行。
