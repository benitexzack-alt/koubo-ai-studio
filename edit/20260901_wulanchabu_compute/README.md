# 乌兰察布算力口播工程

当前阶段：新版纸艺导演 v3 已完成双提示词拆分试跑与拍摄前校验，等待首帧生成、RunningHub 人工图生视频和原片实录重绑。

- `00_工程控制`：用户原稿、事实锁、生产状态与回执。
- `01_原片待绑定`：等待用户拍摄完成后放入原片；原片音频将成为字幕唯一正文。
- `02_官方素材`：政府、央媒和企业官网的来源存档、图片与视频。
- `03_导演拆解/paper-v3-r3`：当前唯一有效的预拍导演请求、路线锁和双提示词交接验证；10 个首帧提示词与 10 个 RunningHub 图生视频提示词严格一对一配对。
- `edit/generated-video/20260901_wulanchabu_compute_paper-v3-r3`：首帧自动化 JSON、RunningHub 图生视频 JSON 和人工复制清单的唯一交接入口。
- `03_导演拆解/paper-v3-r2`：上一版已验证预拍历史，只保留原始混合执行单证据，不再作为当前提示词交接入口。
- `03_导演拆解/paper-v3-r1`：首次编译历史，因执行单生成器末尾空行缺陷在进入生产前作废，不得晋级。
- `03_导演拆解` 根目录旧版文件：仅作事故历史证据，已由 `00_工程控制/wrong-route-supersession.v1.json` 禁止进入当前生产。
- `04_V8规划`：真人画面上的 V8 语义卡、音效与视觉节奏。
- `05_动画资产`：旧联系样片仅作历史证据；新纸艺动画必须由 `paper-v3-r3` 双提示词计划生成，不伪装成真实建设画面。
- `06_转写与字幕`：收到原片后建立实录字幕和修正记录。
- `07_预览与质检`：低清候选与人工验收材料。
- `08_正式成片`：仅在用户确认低清候选后生成。
- `09_发布包`：正式有声候选生成后一次性整理。

原始素材只读，不覆盖、不删除、不重命名。

当前预拍计划不能直接正式渲染。拍摄完成后必须以实录声音为正文重新生成 post-shoot 计划；任何未命中纸艺路线的段落必须明确阻塞，不能自动回退成普通标题卡。
