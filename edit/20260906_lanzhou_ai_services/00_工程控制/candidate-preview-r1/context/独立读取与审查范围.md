# 候选知识上下文与独立读取

本条：20260906_lanzhou_ai_services。知识任务独立身份为 opc-task-20260907-lanzhou-candidate-preview-r1-context；原导演身份 task-20260906T154937Z-69f6ebf8 不变。

## 技术门

官方增量索引已成功执行，不带 --full；官方 start 生成实际 KB context，validate-context 返回 context-valid。三字段消费入口为同目录 context-binding.v1.json。知识门的 formal_execution_allowed=true 不构成渲染授权，permission 中 formalEnabled、productionEligible、publishAuthorized、userPreviewApproved 仍为 false。

最初扩展读取框架的 blocked 结果和未读清单已保留。按父任务后续明确的 prepare-request 接口，本阶段绑定已存在的候选入口、导入数据、视觉计划和实际适用知识材料；request 不能成为生成它所需的 context 的循环前提。完整 request 与 runner 闭包由后续独立审查绑定，不提前声明通过。

## 实际应用

- 完整读取本地候选入口、composition、419行独立动效副本、局部tsconfig、全部visual-plan、全部v2派生选择及消费manifest。
- data JSON读取范围在 read-receipts.v1.json 明示：完整papers/timeline及候选边界、57条字幕文本与时点、13项疑点等。没有逐项阅读1019词的全部嵌套来源证据，不假称原速听过。整文件SHA是输入身份绑定，不是音频正确性证明。
- P01至P05唯一映射和原有动作缺陷保留；当前授权是本条接受已披露缺陷，不是旧QA结论被改成PASS。B23为原录缺省，首帧已显示纸面标签不等于逐字入场。
- P02跨入试用主题、P03覆盖培训主题、P05位于任务举例之后，均按资料中披露的上下文小结理解。纸艺原声在composition中保留为0.1增益，仅确认代码消费，不确认实际可听质量。
- 账号preflight为ready-historical-stale。全账户36作品、最近一条576播放等仅历史参照，不据此缩短本条完整实录，不以一次表现推断因果。

## 审查边界

当前完成的是上述已读源码与数据的独立初步研判，以及另存的静态消费比对。未发现可仅凭这些文件认定的新增阻断缺陷，但完整runner、request、依赖闭包、真实输出和安全区仍须审查；此句不构成allow。

未把父任务报告的tsc成功或9项visualtests当作本worker运行结果。未生成小样、未试听、未核验最终字幕遮挡、未核验最终总线峰值。13项ASR仍待人工确认，尤其带有候选文字的语义叠层不得包装成全部逐字准确。

本轮只写context目录和获准的官方RAG派生缓存/新任务context；没有改共享源码、历史人工context、原字幕或资产，没有commit、上传、生成付费或知识结论晋级。
