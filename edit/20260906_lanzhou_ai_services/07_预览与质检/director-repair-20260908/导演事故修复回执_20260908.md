# 纸艺导演事故修复回执

日期：2026-09-08。用户批准：“好的，那我们开始修复吧。”
修复前提交：`4d89544a286d3000fd0ab1782ab6f6f40263a345`。
状态：**本地修复与回归已通过，真实新动态验证未完成，不能声明生产全链已验收。**

## 一、实际修复

| 已批准修复项 | 当前实现 | 已取得证据 |
|---|---|---|
| 1. 动作必须符合原意 | 逐件初态、允许/禁止流向、前提、动作与终态结构合同；首帧和动作提示词由同一合同编译，独立机制审阅绑定原句及完整机构SHA | 作废入库、带字牌运动、先后关系、规划遮字等变异拒绝；改完整机构再重编译也不能继承旧机制审阅 |
| 2. 文字与运动分离 | 固定独立文字支架；无字活动件；规划运动范围不得侵入文字和字幕区；每短镜最多4活动动作，不再强凑4步 | 合法机构通过，折叠文字载体、标签碰撞、字幕侵入、首帧自由追加反向关系被拒绝；真实模型保持能力仍待实测 |
| 3. 动态先试再扩 | 单镜试验可选P01/P02等；静态批准绑定当前带字图；动态验收按完整动作合同SHA核对；已通过样片直接复用 | 原生编译、首帧准备、本地烘焙OCR、交接及V9交接状态联测通过；不再重复生成已接受试验片 |
| 4. 动作过程验收 | 全段0.5秒网格、动作段0.25秒及边界前后帧；真实probe、全解码、源帧像素核验；每节点原始OCR证据；语义与静音观看分开绑定 | 真实P05的67源帧核验通过，60帧冒充48帧被拒；P03/P05既有遮字、消字负例被拒，不能用首中尾通过抵消 |
| 5. 阶段衔接 | 未说节拍可据完整实录审阅诚实省略；首次可读与后续强调分离；首次候选只需已有输入，不再依赖未来候选验收；回执内部类型/状态/依赖重验 | 39项实录修订测试、58项隔离门禁编排测试；六个真实编译/校验CLI及素材采集接到首次候选构造函数 |
| 6. 失败不当成功 | 五条源视频SHA固定为负例；例外、失败状态不能进入可复用成功记录；跨任务/版本/图片变化须重新验 | 负例改名仍拒绝；失败内层回执即使外层批准且哈希正确也被拒绝 |

“固定文字支架”和动作数量上限是本次保守试点，不是模型能力的统计结论。纸艺立体风格保留，未改成无字动画或普通Remotion信息卡；交付给图生视频的首帧仍应已经带字。

## 二、验证结果

- 最终聚焦回归：**28个测试程序中26通过、0失败、2跳过**，计数单位为程序，不冒充28条真实视频或全部断言数量。命令：`node tools/test-director-incident-repair.mjs --output <新回执路径>`。
- 权威结果：`local-regression.v2.json`，SHA-256 `53374320186fffcb944e1a761c0f2a33340611fd3a5707e122cddfb1910e565a`。v1保留为较早回归证据，不覆盖。
- 两项跳过：`test-request-isolation`、`test-supervisor-a-acceptance-gate` 缺少 `KOUBO_DIRECTOR_ACCEPTANCE_EVIDENCE_ROOT` 对应历史外部验收材料；未伪造通过。独立签名、退役输入及生产门其他测试正常通过。
- 原生首候选联测：6个真实CLI、36源帧、144项真实Tesseract OCR、1129个绑定文件；无领域校验器替身。静音原片、中文标签片及人工审阅输入明确为隔离合成测试，不是真人口播或生成模型动画。
- 首候选工具闭包：15项断言通过，包含真实4工具身份、外部路径、符号链接、附加字段和2501文件规模测试。独立复核发现的 `tools` 容器漏收已修复并再次拒绝；普通素材路径边界未放宽。
- 首帧交接域真实原生链已验证到 `generation-handoff-ready`；V9第4至9阶段所有原生制品的兼容闭环尚未全量证明。首次候选输入构造的独立联测不等于V9完整状态机真实节目已走通。
- 代码静态检查、当前活动档案与包锁校验另外留在本目录的实现清单。当前导演版本仍为9.1.0，事故策略版本为1，不借修复擅自宣布新正式版本。

## 三、独立复核与诚实边界

本轮分工审查了动作合同、实录、真实媒体、首帧交接与V9状态、首次候选门。父级复核发现并补齐“只有媒体自报JSON”“新生产者与旧消费合同不兼容”“真实工具路径被当越界”等问题；最终独立复核在被指定的机构锁和工具容器修复点未发现新增P0/P1，不表示穷尽全仓无缺陷。

机制审阅的不同姓名、SHA和声明只能证明一致性，不能证明审阅人确实理解画面；抽样与OCR也不能证明每一帧永不故障。OCR受压缩和裁剪影响可能误拒绝，须复核真实画面，不能把空结果直接说成错字。工具SHA只覆盖实际可执行文件，不等于第三方发行签名、动态库与环境完整证明。

代表复用目前仅允许完整动作合同SHA完全相同。不同原句、部件或机制审阅通常使合同不同，因此可能逐镜试验；本轮没有实现跨镜“相似机构自动泛化”。每次通过的试验视频直接保留使用，不为批量再生成一遍。

## 四、不变性与接入

- 原预拍request、plan、validation，五段事故源视频和现有正式文件共9项SHA与审计锚点完全一致；证据见 `immutability.v1.json`。未重生成、覆盖或删除历史媒体。
- V8活动生产档案与HEAD一致；旧微信事故job仍 `formal.enabled=false`。该旧job本身不在当前HEAD中，回执仅记录当前关闭状态和SHA，不伪称Git对比通过。
- 两个已安装Skill入口均为当前仓库符号链接，修复已在本地入口可读；未覆盖另一套Skill。
- 导演包锁：104文件，包树SHA-256 `443128caf3c500134115b4a1cfa64621c49a4f914f34396cbfdf43705e41980e`；当前档案、精确树与漂移拒绝校验通过。旧动态验收锚点未被替换成“新代码已复现”的声明。
- 本机仍缺 `/Library/Application Support/KouboDirector/director-independent-ed25519-trust-root.v2.json`。本轮未安装或伪造信任根。真实受控预览仍须合法独立授权、信任根、当前知识上下文和素材证据；正式渲染与公开发布仍分别受用户授权约束。
- 未登录、上传、生成、付费RunningHub，未重跑本期正式片，未修改稳定V8组件或Shotcraft正式片，不替用户发布。

## 五、下一步最小验证

1. 为P01式分流和P05式前提机构建立新的试验修订，先锁原话、固定文字牌和最多4个必要动作；原事故输入不覆盖。
2. 先走本地机构审阅与带字首帧验收。真实外部生成另取得用户对范围与费用的授权，不自动重试。
3. 每个新机构先一条动态样片，原速静音可讲明白、有声与口播一致、文字全程可读后再推进；合格试验片直接复用。出现反义、遮字或消失就停止，不再批量放大。
4. 新实录到齐后重新对位，小样由用户完整观看；只有此真实链路取得验收，才能说本轮修复在生产中有效。

完整修改文件与当前SHA见 `implementation-manifest.v1.json`；Git只暂存本任务明确拥有的文件，不纳入其他任务的Remotion、稿件或媒体改动。
